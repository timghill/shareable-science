"""Provide additional functionality to core module."""

import numpy as np

def helper1(x, flag=True):
    """Help run simulations.

    Parameters
    ----------
    x : float
        Simulation parameter setting. Between [0, 1]
    flag : bool
           Flag to control simulation behaviour
    
    Returns
    -------
    np.ndarray
        Simulation outputs.
    """
    pass